{
  "name": "@tarileo/artifact-store",
  "version": "0.1.1",
  "description": "Shared artifact storage: stable repoId, manifest, local-fs provider. Single owner of bytes for CLI + pi-extension + future R2/worker.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist/"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.11.0"
  },
  "publishConfig": { "access": "public" },
  "license": "MIT"
}
